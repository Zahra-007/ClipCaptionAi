import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  weight: ["300", "400", "500", "600", "700", "800", "900"],
  display: "swap",
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "ClipCaptionAI – Add Captions to Your Videos in Seconds",
  description:
    "Upload your video and our advanced AI will instantly transcribe the speech and burn beautiful subtitles into it.",
  openGraph: {
    title: "ClipCaptionAI",
    description: "AI-powered video captioning in seconds.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>{children}</body>
    </html>
  );
}
